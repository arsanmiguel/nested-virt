# Local overrides (not committed). Copy: cp config.local.env.example config.local.env

KEY_NAME=your-ec2-key-pair-name

# Optional: Windows ISO for deploy-hyperv-guest.sh
# WINDOWS_ISO_S3_URI=s3://nested-virt-bootstrap-${AWS_ACCOUNT_ID}/nested-virt/Win2022.iso
